import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  movementContainer: {
    height: 150,
  },
  centeredText: {
    textAlign: 'center',
  },
  rep: {
    marginTop: 10,
  },
});
